# 변경 이력

버전마다 **무엇이 달라졌나**. 배포하는 절차와 어느 레이어를 올렸는지는
[deploy/RELEASE-CHECKLIST.md](deploy/RELEASE-CHECKLIST.md) 에 있다 — 받는 사람이
볼 것과 배포하는 사람이 볼 것은 다르다.

버전은 **최하위 자리만** 올린다. `v0.4.9` 다음은 `v0.4.10` 이다 — 9 다음은 10이지
올림이 아니고, major·minor 는 사람이 명시적으로 요청할 때만 올린다.

---

## v0.5.8 — 2026-09-23

**보안 — 게이트웨이에 로그인이 생겼다 (켜야 걸린다)**
- ⚠ 지금까지 `/api/ext/v1` 말고는 **인증이 없었다.** 같은 네트워크에서 `:8000` 에 닿는
  누구나 데이터셋을 지우고, 추론을 시작해 팔을 움직이고, Vast 키가 저장된 호스트에서는
  **GPU 를 빌려 돈을 쓸 수 있었다.**
- 설정 → **보안** 에서 비밀번호를 정하면 `/api/*` 와 `/ws` 가 세션 쿠키를 요구한다.
  **안 정하면 예전처럼 열려 있고**(릴리스를 받는 순간 배포된 로봇이 전부 잠기면 그건
  안전 기능이 아니라 사고다), 꺼진 상태는 화면이 계속 경고한다
- ⚠⚠ **E-stop 은 어떤 경우에도 인증을 안 탄다.** 멈추는 것을 막을 이유가 없고, heartbeat 가
  401 이면 estopd 가 "브라우저가 죽었다" 로 읽어 **돌던 추론을 2.5초 뒤 SIGKILL** 한다
- 비밀번호를 바꾸면 다른 기기의 세션이 전부 끊긴다. 끄는 데도 지금 비밀번호가 필요하다

**클라우드 GPU — 빌리기 전에 막는다**
- ⚠ **게이트웨이 이미지에 `ssh`·`scp` 가 없었다.** 로컬 학습은 ssh 를 안 타고 개발 머신은
  저장소에서 직접 돌아 호스트 것을 써서 오래 안 걸렸는데, `vastai` 는 들어 있어서 **준비
  점검이 초록불**이었다 — 기계를 빌린 **다음에야** "ssh 가 없습니다" 로 끝났다. 베이스
  이미지 `cu130-4` 에 넣고, 준비 점검이 `ssh`·`scp` 를 본다
- **파기 창이 무엇을 잃는지 센다.** 중간 체크포인트는 Hub 에 안 올라가므로 파기하면 그대로
  사라진다. "확인 중 · 못 물어봄 · N개" 세 상태를 섞지 않는다 — 못 물어본 것을 "0개" 로
  말하면 사람이 없다고 믿고 지운다
- **태그 없는 데이터셋으로는 빌리지 못하게 막는다.** 안 막으면 기계를 만들고 스택을 6분
  깔고 나서야 죽는다(돈을 쓴 뒤다) — 아래 학습 절

**학습 — 받은 데이터셋으로 학습이 안 되던 것**
- ⚠ 허브에서 받은 데이터셋으로 학습하면 `meta/info.json` 을 못 찾아 죽었다. lerobot 은
  `~/.cache/huggingface/**lerobot**/<repo>` 를 보는데 받는 자리는 **허브 캐시**였고, 못
  찾으면 허브에 코드베이스 태그를 묻다가 **태그 없는 저장소에서 그대로 죽는다**(폴백 없음).
  로컬 학습은 이제 `--dataset.root` 로 받아 둔 자리를 직접 가리킨다
- ⚠ **화면의 [업로드] 가 태그를 안 달고 있었다.** 녹화 푸시는 lerobot 을 거쳐 달리는데,
  업로드 버튼은 `hf upload` 로 폴더만 올렸다 — 그래서 화면으로 올린 데이터셋은 **임대
  GPU 에서 학습이 안 됐다.** 이제 업로드가 끝나면 `codebase_version` 으로 태그를 단다
- 임대 학습은 시작 전에 데이터셋 태그를 확인한다(모르면 막지 않는다)

**카메라 — 스캔은 되는데 등록만 안 되던 것**
- ⚠ 실기(.120): 데몬 venv 에 **OpenCV 가 없었다.** 열거는 cv2 없이 되고 장치를 여는 것만
  `cv2.VideoCapture` 라 **등록에서만** 죽었고, 그래서 스캔 목록은 멀쩡해 보였다.
  `apply.sh` 가 numpy·opencv 를 **매번**(`--check` 포함) 보고, 못 깔면 빨간 줄로 말한다
- **등록 실패가 사유를 말한다.** camerad 가 이미 말해 주던 것(`Cannot open /dev/video0`)을
  세 겹으로 덮어쓰고 "등록 실패" 한 줄만 남기던 것을 고쳤다

**데이터셋 뷰어 · 허브**
- ⚠ **허브에서 받은 데이터셋은 영상이 전부 404 였다.** 경로 가드가 심볼릭을 따라가 판정해서
  (HF 캐시는 스냅샷이 `blobs/` 로의 링크다) 항상 밖으로 나갔다. 화면은 그걸 "코덱" 이라고
  불렀는데 — 브라우저가 mp4 대신 404 JSON 을 받아 파싱하다 난 오류였다. 이제 링크를 따라가기
  **전에** 판정한다(`..` 은 여전히 막는다)
- 재생 실패 배너가 **브라우저가 한 말**(코드·메시지)을 그대로 보여주고 재시도를 준다
- **허브 다운로드가 진행 상황을 말한다** — `n/N 파일 · % · 속도 · 남은 시간`. 예전에는
  "다운로드 중..." 이 영원히 남았다(지우는 곳이 없었다)
- ⚠ **완료를 세어 보고 말한다.** 받다 만 저장소를 완료라고 하던 것이 실제로 사고였다 —
  영상 파일 하나가 안 왔는데 목록·에피소드 수는 `meta/` 에서 읽어 멀쩡해 보였고, 몇 주 뒤
  영상이 안 나올 때야 드러났다. 빠졌으면 `incomplete` 과 **빠진 파일 이름**을 말한다

**설치 · 문서**
- ⚠ `apply.sh` 가 **깨진 지점 스무 줄 뒤에서** 죽던 것. `set -e` 는 `&&` 왼쪽의 실패를
  봐주므로 venv 생성이 조용히 지나가고 한참 뒤 "pip 가 없다" 로 끝났다. 반쪽 venv 를 다시
  만들고, 전제 검사도 진짜 관문(`ensurepip`)을 본다
- Swagger 문서를 프론트에서 닿는 자리로 (다른 세션)
- 새 문서: [카메라](docs/camera-troubleshooting.md) · [에피소드 뷰어](docs/dataset-viewer-troubleshooting.md) ·
  [게이트웨이 로그인](feature/gateway-auth.md) · [허브 다운로드 진행률](feature/hub-download-progress.md) ·
  [문서 지도와 정리 계획](docs/doc-map.md) · [현장 배포·운영 검토](feature/field-deployment.md)
- README 에 **Google Colab 으로 학습하는 길**을 적었다 — 데이터셋 [Hub 업로드] → 공식 ACT 노트북
  → 저장소 [다운로드]. Colab 은 API 가 없어 화면 연동은 못 한다(feature/field-deployment.md §6)

**바탕화면 아이콘 — 주소를 외우지 않는다**
- 설치가 앱 메뉴와 바탕화면에 **[Piper Studio]**·**[Piper Studio 진단]** 을 만든다
  (`install-shortcuts.sh`, `apply.sh` 5절). [Piper Studio] 는 웹을 열고 — Chrome 이 있으면 주소창
  없는 앱 창으로 — 프론트가 응답하지 않으면 **진단 보고서를 대신 연다.** "연결할 수 없음" 흰
  화면을 일반 사용자에게 보여 주지 않는다
- [Piper Studio 진단] = `piper-doctor.sh`: 적용본·웹 응답·컨테이너·데몬 유닛·`apply.sh --check`
  의 ✗ 줄·저널을 한 장으로. 아무것도 바꾸지 않고, 명령이 없어도 죽지 않는다
- 제거(`piper-uninstall.sh`)가 같은 자리를 지운다. 탭·북마크 이름이 `frontend` 였던 것 → Piper Studio

⚠ **베이스 이미지가 `cu130-3 → cu130-4` 로 바뀌었다**(`openssh-client`). 파일 끝에 붙여서
앞 계층은 그대로라 새로 받는 것은 그 한 층뿐이다.

## v0.5.7 — 2026-09-21

**시뮬 · 안전 — 죽은 프로세스가 팔을 영영 잠그던 것**
- ⚠ **명령 세그먼트가 남아 있으면 그 팔은 다시 못 몬다**는 상태가 있었다. .120 의 시뮬 팔이
  그랬다: 추론도 녹화도 릴레이도 안 도는데 조종 창이 계속 "누가 이미 쥐고 있습니다"로 막혔다.
  SIGKILL 당한 프로세스(E-stop 이 녹화를 죽이는 경로가 그렇다)는 close 를 못 해 `/dev/shm` 에
  파일을 남기고, **존재 자체가 lease** 라 그 위에 못 연다
- ⚠ **[해제]·[연결]도 데몬 재시작도 안 먹었다.** 남은 파일은 **root** 것이다 — 게이트웨이는
  컨테이너 root 로 돌고 `/dev/shm` 을 호스트와 공유하는데, `/dev/shm` 은 sticky 라 사용자로
  도는 데몬의 `unlink` 가 EPERM 이다. 그걸 **조용히 False 로 삼켜** 브리지는 지운 줄 알았다
  (실측: `.action` 은 root 소유로 2시간째 그대로, 옆의 `.state` 는 같은 팔이 10ms 마다 쓰는 중)
- **이제 존재가 아니라 마지막 기록 시각으로 판정한다.** 5초 넘게 아무도 안 쓴 세그먼트는 죽은
  것으로 보고 지우고 이어받는다 — 살아 있는 소비자는 데드맨(300ms)보다 훨씬 자주 쓴다. 치울
  수 있는 쪽(만든 쪽)에서 판정한다. 방금 쓴 소유자는 그대로 거절하고, 못 지우면 **먹는 명령**을
  문구에 담는다
- simd 의 기동 정리가 **자기 팔 세그먼트를 한 번도 안 지우고 있었다.** 필터가 `".sim_" in n`
  인데 이름은 접두사가 빠진 `sim_follower1.action` 이라 영영 안 맞았다. 그 자리를 지키던
  테스트도 **그 문자열이 소스에 있는지**만 봐서 초록이었다 — 이제 진짜 이름으로 판정을 부른다

**로봇 — SO-101 을 돌려 놓고 쓰기**
- **리더를 작업대에서 180° 돌려 놓고 잡는 구성**을 고를 수 있다. 관절 매칭은 부호 다섯 개짜리
  표이고 반 바퀴는 그중 **둘만** 바꾼다: 좌우(joint1)는 끝단이 회전축 반대편에 앉아서, 손목
  롤(joint6)은 축이 조종자를 향하게 돼서. **피치 셋은 안 바뀐다 — 중력은 안 돌아간다**
- 돌려 놓았다는 것은 그 기계의 물리적 사실이라 좌/우와 **같은 자리**(so101d 세션, 어댑터
  열쇠)에 남는다. 재연결·재기동에도 유지된다
- ⚠ **릴레이가 도는 중에는 못 바꾼다.** 부호가 바뀌면 같은 리더 각도가 다른 방향의 목표가 되어
  팔로워가 그 자리에서 튄다 — 앵커는 오프셋만 흡수하지 방향은 못 흡수한다
- **녹화도 같은 표를 탄다.** 릴레이만 고치면 조종은 맞는데 데이터셋만 거울상이 되고, 그건
  학습까지 가서야 보인다

**화면**
- **E-stop 이 상단 메뉴바 맨 끝으로 왔다.** 오른쪽 아래 원이던 자리의 이유("어느 페이지든 같은
  자리")는 그대로 지킨다 — 맨 끝이라 x 위치가 **창 너비에만** 달리고(앞의 칩들이 늘고 줄어도
  안 밀린다), `shrink-0` 이라 창이 좁아져도 안 줄어든다

**문서**
- **README 를 밖에서 보는 사람 기준으로 다시 썼다.** 설치 41% · "이게 뭐 하는 물건인가" 4%
  이던 것을, 한 바퀴(수집→학습→추론→평가)와 실측 숫자와 안전 설계가 먼저 오게 바꾸고 설치는
  뒤로 내렸다. 클라우드 GPU·가상환경 편집기 화면도 실었다

**클라우드 학습** (다른 세션)
- **빌린 기계에서 받아 온 학습이 로컬 학습과 같은 폴더 모양으로 앉는다.** 예전에는 한 번의
  학습이 목록에 **둘로** 보였다(중간 체크포인트와 최종 가중치가 다른 경로로 내려왔다). 이름은
  우리가 지을 것이 아니었다 — 기계도 같은 lerobot 을 돌리므로 이미 이름을 지어 뒀고, 업로드된
  `train_config.json` 의 `output_dir` 에 그 주소가 실려 온다

## v0.5.6 — 2026-09-21

**카메라 · 조명**
- **조명 경보를 카메라마다 켜고 끈다.** 손목 카메라는 팔과 같이 움직여 보는 장면이 계속
  바뀌고, 조명 판정은 그걸 조명 변화로 읽는다 — 경보가 **맞는 말이지만 쓸모가 없고**,
  쓸모없는 경보는 옆의 진짜 경보까지 묻는다. 카메라 설정 패널의 체크박스이고, 끈 상태는
  세션에 남는다(껐는데 재시작하면 또 뜨는 것은 안 끈 것보다 나쁘다)
- ⚠ **끄는 것은 경보뿐이다.** 밝기·색 측정과 발행은 계속하므로 수집·추론 화면의 실시간
  표시와 에피소드 뷰어는 그대로다. 판정도 계속 돌려서, 다시 켜면 **지금** 이상한 것을 그
  자리에서 말한다

**클라우드 GPU · 학습**
- ⚠ **저장한 Vast API 키가 CLI 호출 두 곳에 안 실려 있었다.** 등록 버튼과 등록 확인이
  빈 환경으로 CLI 를 띄워, 그 둘만 CLI 의 **오래된 저장 파일**을 보고 있었다. 확인이
  `401 Invalid user key` 로 답하던 것도, 등록 버튼이 **엉뚱한 계정에** 등록할 수 있었던
  것도 같은 원인이다 — 사람은 방금 등록했다고 믿는다
- **중간 체크포인트를 가져올 수 있다** (기본 꺼짐). `save_freq` 대로 네 개가 기계에
  쓰였는데 회수는 **마지막 하나만** 보고 있었다(Hub 는 학습이 끝날 때 한 번 올리므로
  중간 것을 아예 못 본다). 켜면 `checkpoints/*/pretrained_model` 을 전부 내린다 —
  개당 약 200MB, 네 개에 대략 $0.014. Hub/scp 판단 **앞**에서 돈다: 뒤면 늦다(성공적으로
  올린 학습은 바로 파기로 가고, 중간 체크포인트는 기계에만 있다)
- **조용한 3~4분이 무엇을 하는 중인지 말한다.** 화면은 그 구간을 "접속 기다리는 중"이라
  불렀지만 시간은 **스택 설치**에 간다(slim 이 거기서 torch 를 받는다). 서버는 계속 찍고
  있었고 화면에만 안 왔다 — 그래서 SSH 문제처럼 보였다. 진행 문구는 **골라서** 낸다
  (bootstrap 출력을 그대로 흘리면 `bash: warning: setlocale…` 이 진행 상황으로 뜬다)

**릴리스**
- `release.sh` 가 **이미 나간 번호를 다시 끊으려 하면 굽기 전에 말한다.** 호스트의 "새 버전
  확인"은 버전 문자열을 견주므로, 이미 그 번호를 적용한 기계는 "최신입니다"를 보고 **스스로
  안 받는다** — v0.5.5 에서 실제로 그랬다(.120 이 받은 뒤 같은 번호로 세 번째 발행).
  막지는 않는다(아무도 안 받았으면 다시 끊는 것이 맞다)

## v0.5.5 — 2026-09-18

빌린 GPU 로 학습하는 길이 **눌러서 되는 것**이 됐다. 실기로 여러 번 빌리고, 죽이고, 끊어
보면서 나온 것들이라 대부분의 항목 뒤에 실측 숫자가 있다. 시뮬레이터 쪽은 테이블 위 물건을
화면에서 고칠 수 있게 됐다.

**클라우드 GPU — 빌리는 곳과 학습하는 곳을 갈랐다**
- **클라우드 GPU 페이지는 기계를 만들고**, 학습 페이지는 **빌려 둔 기계를 골라** 학습을 건다.
  학습 설정(데이터셋·스텝·배치·AMP)은 학습 페이지 한 곳에만 있다 — 폼이 두 곳에 있으면
  반드시 어긋난다
- 학습 페이지에 **[로컬] [클라우드]** 실행 위치. 확인 창이 **실제로 도는 명령**을 그대로
  띄운다(서버가 만든다 — 화면이 숫자를 베껴 적으면 언젠가 어긋난다)
- ⚠ **빌린 기계는 자동으로 안 꺼진다.** 학습이 끝나도 남는다 — 끄는 것은 인스턴스 탭에서
  사람이 한다. 대신 학습 없이 20분 넘게 떠 있으면 **지금까지 얼마 나갔는지와 함께** 경고한다
- ⚠ 예전 [빌리기]는 `steps=5000 · batch=8` 로 돌면서 그 숫자를 화면 어디에도 안 보여 줬다.
  그리고 학습 페이지가 보내는 22개 필드 중 **9개만** 받아 `pretrained_path` 를 조용히 버렸다
  — 파인튜닝을 걸어도 말없이 처음부터 학습됐다. 둘 다 고쳤다

**클라우드 GPU — 돈이 새지 않게**
- **고아 스캐너**가 기동 30초 뒤부터 10분마다 훑는다. 우리 라벨이 붙었는데 아무도 관리하지
  않는 기계를 찾아 **경고한다 — 자동으로 끄지는 않는다**(다른 기계의 게이트웨이가 돌리는
  학습일 수 있다)
- ⚠ 게이트웨이가 임대 도중 죽으면 그 기계는 아무도 관리하지 않는다. 기동 때 임대 주장을
  비워서 스캐너가 그걸 본다 — 실기에서 `kill -9` 로 확인했다($0.586/h 로 살아 있었다)
- **가중치가 저절로 집에 온다.** Hub 에 올라갔으면 **기계를 끈 뒤** 받고(기계를 안 타므로
  요금이 안 나간다), 안 올라갔으면 **끄기 전에** `scp` 로 끌어온다. 같은 저장소로 두 번째를
  돌렸을 때 **지난 회차 가중치를 이번 것으로 읽지 않도록** 올라간 시각을 대조한다
- 학습 페이지·인스턴스 탭이 **같은 판정**으로 고아를 표시한다. 정의가 둘이면 서로 다른 말을 한다

**클라우드 GPU — 고르는 자리에서 말해 준다**
- GPU 호환 판정이 **고른 템플릿의 CUDA 빌드**를 따라간다. cu126 과 cu128 은 담고 있는 커널이
  다르고 **한쪽이 다른 쪽의 상위집합이 아니다**(실측: cu128 은 Blackwell 을 얻는 대신
  맥스웰·파스칼·V100 을 잃는다 — 카탈로그 기준 13종 얻고 8종 잃는다)
- **bf16 경고.** 암페어(cc 8.0) 미만에서는 죽지 않고 **에뮬레이션으로 느려진다** — 그런데
  하필 그게 제일 싼 기계들이다(V100 $0.125/h · RTX 2080 Ti $0.095/h). 이제 그 줄에 뜨고,
  AMP 를 fp16 으로 바꿀 수 있다
- 기종 선택지는 **못 도는 것도 보여 주되** 왜 못 쓰는지 적는다

**학습 이미지**
- ⚠ **접속된 것과 학습할 수 있는 것은 다르다.** slim 이미지는 첫 부팅 때 스택을 깔고, 그
  전에 학습을 걸면 3초 만에 `No module named 'lerobot'` 로 죽는다. 이제 `.ready` 를 보고
  기다린다 — `bootstrap.sh` 가 그러라고 적어 뒀는데 그걸 보는 코드가 없었다
- **화면 기본값이 slim 으로 바뀌었다.** 같은 4090 호스트에서 full(14GB)은 8분 상한까지 pull 도
  못 끝냈고 slim(1.3GB)은 **6분 24초에 완주**했다(pull 16초 · SSH 62초 · 스택 4분 23초)
- 스택 설치가 **torch 를 한 번만 받는다.** 예전에는 깔았다 지우고 다시 깔아 같은 회선에서
  **8155MB/7분59초** 였다 — 지금은 **4240MB/3분53초**
- 새 이미지를 GHCR 에 올렸다(`*-cu126-20260918`). 익명 pull 확인, Vast 템플릿 둘 갱신
- **Vast API 키를 넣을 자리가 생겼다.** 이미지에 키가 구워져 있던 적이 없다 — 기종·가격
  목록은 계정 없이도 나와서 그렇게 보였을 뿐이고, 그 너머는 전부 키가 필요하다
- ⚠ **"확인 못 했다" 를 "등록 안 됐다" 로 말하던 것.** SSH 키는 등록돼 있는데 화면은 계속
  등록 버튼을 누르라 했고, 눌러도 같은 자리로 돌아왔다 — 나가는 길이 없는 안내였다.
  확인 실패는 이제 막지 않는다(막는 쪽이 더 나쁜 오류다 — 제대로 해 둔 사람이 영영 못 빌린다).
  그리고 진짜 원인은 **API 키 권한**이었다: `show ssh-keys` 는 401 인데 `show user` 는 같은
  키로 통과한다. 고칠 곳과 가리키던 곳이 달랐다

**시뮬레이터 — 테이블 위를 화면에서 고친다**
- 테이블 위 물건을 **JSON 으로 정의**하고 장면 편집 페이지에서 고친다. 장면은 **런타임에
  교체**되고, 게이트웨이가 그것을 보관한다
- ⚠ **누가 쓰는 중이면 세계를 안 바꾼다**
- 사람이 스캔·모델링한 **메시를 가져올 수 있다** — 무엇인지 말한 뒤에 받는다
- 에피소드에 **어느 세계에서 모았는지**를 기록한다 (나중에는 알 수 없는 값이다)
- 집기-놓기를 **스크립트로** 돌려 사람 없이 에피소드를 만든다. 데모는 에피소드를 모으는
  자리에 있다
- 시뮬레이터에서 관절 모드를 뺐다 — 그 판정은 백엔드가 한다

**릴리스 · 업데이트**
- ⚠ **업데이트가 멀쩡히 끝났는데 "전제가 빠져 있어 멈췄습니다" 가 뜨던 것.** 로그 전체에서
  `sudo` 로 시작하는 줄을 처방으로 긁었는데, `apply.sh` 에는 멈추는 것과 상관없는 **조언**도
  그렇게 적혀 있다(CAN 이름 규칙 안내). 이제 "아래를 먼저 실행하세요" **뒤의 블록만** 읽고,
  카드도 결과를 보고 말한다 — 성공했으면 "적용은 끝났습니다. 다만 아래는 직접 해야 합니다"
- ⚠ 그 안내가 뜨는 호스트는 **정말로 CAN 이름 규칙을 만들어야 한다.** 없으면 인터페이스가
  `can0`/`can1` 로 붙어, 포트를 바꿔 꽂는 순간 **두 팔의 이름이 뒤바뀐다**
- `release.sh` 가 **올릴 레지스트리를 스스로 안다** (GHCR + 사설). 사람이 환경변수로 고르지
  않는다 — 한쪽에만 올리면 다른 쪽에서 받는 호스트가 옛 버전을 "최신" 이라 본다. 올린 뒤
  레지스트리에 **다시 물어 확인**하고, 못 올린 곳은 끝에서 크게 말한다

**설치 · 데몬**
- ⚠ **이미지가 열리지도 않는 torchcodec 을 싣고 나갔다.** .120 에서 학습이 첫 배치에 죽었다 —
  `libnppicc.so.13: cannot open shared object file`. torchcodec 은 NVIDIA NPP 에 링크돼
  있는데 아무도 그걸 안 깐다(torch 는 NPP 를 안 쓰니 의존성에 없고, torchcodec wheel 도
  선언하지 않는다). LeRobot 의 기본 영상 백엔드가 torchcodec 이라 **영상을 읽는 모든 경로**가
  같은 자리에서 죽는다 — 학습만의 문제가 아니었다. CPU 빌드로도 못 피하고(PyPI 의 같은
  버전도 같은 라이브러리를 찾는다), 깔기만 해도 안 된다(로더 경로에 넣고 `ldconfig` 까지).
  이제 베이스 이미지가 그걸 담고, **빌드가 스스로 `import VideoDecoder` 로 확인한다**
- 기동할 때 깔린 데몬이 **이번 릴리스와 맞는지** 보고, 아니면 무엇을 하면 되는지 말한다
- 건너뛴 릴리스를 따라잡을 때 wheel 을 **이름이 아니라 버전으로** 견준다
- 유닛이 **끝난 것을 알아챈다** — 누가 물어볼 때까지 기다리지 않는다
- Hub 업로드가 `hf` 를 쓴다 (`huggingface-cli` 가 이제 1 로 죽는 껍데기다)
- 릴리스 규칙을 문서에 적고 **규칙마다 그걸 지키는 테스트**를 달았다

## v0.5.4 — 2026-09-15

"개발 머신에는 있는데 배포판에는 없는 것" 이 하루에 세 번 다른 얼굴로 나왔다. 셋 다 고치고,
그 부류를 다시 못 빠져나가게 규칙을 코드에서 끌어내도록 바꿨다.

**카메라**
- 카메라 프로파일 저장이 500 으로 끝나던 것. 게이트웨이 이미지에 카메라 패키지(`piper_cam`)가
  없었는데 게이트웨이 코드는 그걸 프로파일 저장·적용, 조명 감시, 컨트롤 단위, 정렬 태그,
  데이터셋 조명 지표에서 쓴다. 장치를 여는 것은 여전히 camerad 의 일이다 — 설치와 개방은
  다른 층이다
- ⚠ 회색 카드 보정이 "camerad 가 응답하지 않습니다" 로 끝나던 기계는 **데몬 소스가 낡은**
  것이었다. 아래 설치 항목이 이번 적용에서 따라잡는다

**데몬 · 설치**
- 데몬 호출이 실패하면 **진짜 사유**를 말한다. 한 문구가 ① 죽음 ② 타임아웃 ③ 동사를 모름
  (옛 데몬)을 다 덮고 있었다. ③ 은 재시작으로 영영 안 변하는데 화면이 그걸 말하지 않아
  사람이 엉뚱한 데를 팠다. camerad·rsd·robotd 셋 다 같은 규율
- **건너뛴 릴리스를 따라잡는다.** `daemons=` 는 그 릴리스에서 바뀐 것만 말하고, 적용된 트리는
  첫 설치 뒤 늘 존재해 "없으면 푼다" 도 다시는 안 걸렸다 — 한 번 뒤처지면 영원히 멈췄다.
  이제 깔린 데몬 소스를 번들과 견주고, 표시가 없는 옛 호스트는 낡은 것으로 본다.
  점검(`--check`)도 말한다
- ⚠ **이번 적용은 데몬을 재시작한다** (뒤처진 기계는 소스를 새로 푼다)

**클라우드 (임대 GPU 준비)**
- 게이트웨이가 **자기 SSH 키**를 만든다. 사람의 `~/.ssh` 를 빌리면 저장소에서 띄웠을 때만
  되는 기능이 된다 — 컨테이너에는 그 디렉토리가 없다. 자리는 `/data/config/ssh` 라 재설치를
  넘어 남고, 비밀키는 화면에 안 나간다
- 설정에 **클라우드 탭** — 자격증명·연결 상태를 한 자리에서
- 학습 이미지를 GHCR 에 공개했고(익명 pull 확인) Vast 템플릿 둘을 등록했다. `vastai` CLI 가
  게이트웨이 안으로 들어왔다 — 배포판 사용자는 그걸 따로 깔 방법이 없다
- ⚠ 그 CLI 가 딸린 패키지를 여럿 끌고 온다(PDF·이미지 계열). backend 이미지가 그만큼 무거워진다

## v0.5.3 — 2026-09-15

로봇 등록의 [연결] 이 `piper_sdk not installed` 로 끝나던 것. 설치가 그 패키지를 한 번도
확인하지 않았다 — CAN 은 멀쩡히 올라와 있는데 팔만 못 열었다.

**설치**
- 데몬 venv 에 `piper_sdk` 를 **직접 깐다** (컨테이너와 같은 핀 0.6.1). 예전에는 venv 를
  `--system-site-packages` 로 만들며 "시스템에 있겠지" 로 뒀는데, 개발 머신엔 있었고 맨
  우분투(NUC)엔 없었다. wheel 은 `--no-deps` 로 깔리고 팔 여는 코드는 늦게 import 하니
  어느 검사에도 안 걸렸다
- `./apply.sh --check` 항목에도 들어간다 — 설치가 "다 됐다"고 한 뒤 [연결] 에서만 실패하는
  일이 없다
- ⚠ 선택 데몬 의존(mujoco·Feetech SDK)과 달리 **실패를 경고로 넘기지 않는다.** 이게 없으면
  팔이 통째로 안 된다

## v0.5.2 — 2026-09-15

CAN 이 멀쩡히 올라와 있는데 화면은 "안 올라왔다"고 말했다. 셋 다 같은 원인이다 —
게이트웨이는 컨테이너라 `can0` 이 아예 안 보이는데, 거기서 CAN 을 직접 읽고 있었다.

**로봇 (데몬 재시작 필요)**
- 포트 카드가 bitrate·버스 상태·Rx/Tx 를 빈칸으로 보여 **초기화가 안 된 것처럼** 읽혔다.
  같은 순간 robotd 는 `bitrate 1000000 · ERROR-ACTIVE · 오류 0` 으로 보고하고 있었다.
  버스 상태 탭이 하던 대로 robotd 에게 묻는다
- [DOWN] 이 `bring-down failed: Cannot find device "can0"` 로 끝나던 것. UP 은 처음부터
  robotd 를 거쳤는데 DOWN 만 컨테이너에서 돌고 있었다 — 같은 줄의 두 버튼이 서로 다른
  기계에서 실행됐다
- ⚠ **조작 시작 가드가 배포판에서 조용히 통과하고 있었다.** 버스가 BUS-OFF 여도 못 읽어
  "정상"으로 답했다 — 조그가 열리고 슬라이더는 움직이는데 팔만 안 움직인다. 그 가드를
  둔 이유가 정확히 그 상황이다. 이제 robotd 가 답한다(데몬이 없으면 예전처럼 통과)

⚠ robot wheel 과 데몬 소스가 바뀐다 — `apply.sh` 가 robotd 를 재시작한다.

## v0.5.1 — 2026-09-15

시뮬 파지가 붙잡히고, 조종 창이 키보드만으로 돌고, 수집 정지가 조용히 실패하지 않으며,
다른 기계에서 CAN 을 올릴 수 있다.

**시뮬레이터** (simd 재시작 필요)
- 비스듬히 잡아도 놓치지 않는다 — 손이 10~20° 기울어진 채 나르면 놓치던 것을 마찰콘
  elliptic(올려 둔 impratio 가 그제야 뜻을 가진다)·그리퍼 힘 3배·손가락 댐핑으로 잡았다.
  마찰계수는 요청대로 올렸지만 측정상 차이는 없었다
- 탑뷰 카메라를 0.9→0.6 m 로 내려 큐브가 1.5배 크게 보인다(21→33 px). 통과 팔 베이스는
  화면 안. ⚠ 그 전에 찍은 시뮬 데이터셋·체크포인트와 관측이 다르다
- 시뮬 카메라가 정확히 15 fps 로 발행한다(14.8 이었다) — 수집의 "14.7 Hz" 경고가 사라진다

**조종 창**
- EE 이동을 키패드로도: 8/2 앞뒤, 4/6 좌우, +/− 위아래(방향키·본체 −/= 도 같다). 마우스를
  끌며 QWEASD 로 회전하는 동시 조작은 그대로
- 0 키 = 그리퍼 토글, 한 번 누르면 2초에 걸쳐 닫히거나 열린다. 손으로 만지면 풀린다
- "마우스 끄기" 체크박스(EE) — 켜면 마우스는 무시하고, 클릭 없이 조종 키가 곧 시작이다
- "수집 중" 상태에 [수집 정지] 버튼

**수집**
- 정지·저장하고 다음·재녹화가 실패하면 이유를 띄운다. ESC 정지가 실제로 동작한다
- 브라우저 요청에 시간 제한과 폴링 단일 비행 — 게이트웨이 재시작 뒤 요청이 수백 개 쌓여
  정지 버튼이 브라우저 밖으로 나가지도 못하던 사고(ERR_INSUFFICIENT_RESOURCES)를 막는다

**설치**
- 로봇 등록 [UP] 이 `sudo: a password is required` 로 끝나던 기계 — 설치가 robotd 의 CAN
  명령(`modprobe gs_usb`·`ip link set can*`)만 비밀번호 없이 허용하는 sudoers 를 안내한다.
  ./apply.sh 를 다시 돌리면 그 명령이 찍힌다. robotd 의 오류 문구도 처방을 가리킨다

**학습 이미지 (준비물, 아직 GHCR 에 안 올림)**
- 임대 GPU 용 학습 이미지 full/slim 두 종과 인스턴스 환경 체크 스크립트
  (`./deploy/build-train.sh`, feature/vast-training.md)

## v0.5.0 — 2026-09-14

릴리스마다 백엔드 이미지 30 레이어 중 11개(390MB)가 새로 구워져 새로 내려갔다 —
바뀐 소스는 몇 MB 인데. 레이어가 새로 잘리는 릴리스라 minor 를 올렸다.

**배포**
- 백엔드 이미지의 레이어 순서를 바꿨다. 버전 ENV 는 모든 RUN 뒤로(맨 위에 있으면
  태그가 바뀔 때마다 그 아래 pip install 전부가 캐시 미스였다), 백엔드 의존성 162MB 는
  소스보다 먼저(소스 한 줄에 의존성을 다시 깔았다). 이제 소스만 바뀐 릴리스는 몇 MB 만
  새로 굽고, 업데이트하는 기계도 그만큼만 받는다
- ⚠ **이 버전 한 번은 예전처럼 크게 받는다** — 레이어가 새 경계로 잘려 v0.4.19 와
  겹치지 않는다(390MB 쯤). 그 다음 릴리스부터 작아진다
- 설치되는 패키지는 v0.4.19 와 같다 — 160개, `pip check` 결과까지 바이트 단위로 동일.
  동작은 바뀐 것이 없다

## v0.4.19 — 2026-09-11

USB 웹캠으로 회색 카드 보정을 누르니 "Not a RealSense id" — 보정이 RealSense 에만 있었다.

**카메라**
- 회색 카드 보정(측정·보정)이 camerad(V4L2 웹캠)에도 생겼다. 절차는 rsd 와 같고 —
  자동 노출 수렴 → AWB 끄기 → 자동 끄기 → 카드로 WB 직접 → 밝기(노출 ×100µs 또는
  gain) 비례 보정 → 보고 — 컨트롤 이름만 V4L2 다(`auto_exposure` 는 3=자동·1=수동).
  값싼 웹캠에 수동 WB·gain 이 없으면 그 단계는 건너뛰고 결과에 "건너뜀: …"으로 말한다
- 게이트웨이의 두 엔드포인트가 카메라 종류 분기(`camera_manager`)를 안 거치고 rsd 로
  직행하던 것을 바로잡았다 — 시뮬 카메라처럼 보정이 없는 종류엔 "지원하지 않는다"
- ⚠ camerad 가 새 동사를 알아야 한다 — 이 릴리스는 `cam` wheel 과 데몬 소스를 올리고
  apply.sh 가 camerad 를 재시작한다

## v0.4.18 — 2026-09-11

NUC 에서 웹 [업데이트]를 누르니 `받기 스크립트가 없습니다: …/v*/piper-install.sh — 이 번들이
낡았다`. 번들은 낡지 않았다 — 자리가 달랐다.

**업데이트**
- README 대로 **버전 없이** 깔면 `piper-install.sh` 가 번들을 `~/piper-web-deploy/latest/` 에
  풀었는데, unitd 는 `v*/` 이름 디렉토리만 봤다(.120 은 늘 버전을 적어 깔아 안 걸렸다).
  이제 `latest` 로 받아도 매니페스트의 version= 으로 **버전 이름 디렉토리**에 풀고 `latest`
  는 그 디렉토리를 가리키는 링크로 남긴다. unitd 도 옛 설치의 `latest/` 실제 디렉토리를
  매니페스트 버전으로 읽어 목록·레지스트리·받기·적용에 쓴다. 오류 문구는 "받아 둔 번들이
  없다 — 터미널에서 한 번 `./piper-install.sh`" 로

## v0.4.17 — 2026-09-11

NUC 에서 설치는 끝났는데 시뮬 팔이 안 움직였다 — 큐브는 옮겨지고 카메라는 나오는데.

**팔 명령 경로**
- 팔로워 action 세그먼트를 게이트웨이 **컨테이너(root)** 가 `0600` 으로 만들어 호스트의
  simd(일반 사용자)가 `Permission denied` 로 못 열었고, 그 예외가 `ArmSegmentError` 가
  아니라 명령 스레드가 조용히 죽어 팔이 영영 정지했다. robotd 도 같은 코드라 실기도
  같은 함정이다(.120 에서 왜 걸리지 않았는지는 확인 못 했다). 세그먼트는 umask 와
  무관하게 `0644` 로 만들고(쓰기는 소유자만), simd·robotd 는 열기 실패를 한 번 경고하고
  재시도한다. ⚠ 호스트가 파일을 먼저 만들어 두는 우회는 안 된다 — `fs.protected_regular`
  가 sticky 디렉토리에서 남의 파일을 O_CREAT 로 여는 것을 root 에게도 거부한다
- apply.sh 가 wheel 을 올린 뒤 **켜져 있던** 선택 데몬(simd·so101d)도 재시작한다 —
  아니면 고친 코드가 안 돈다

**제거**
- `piper-uninstall.sh` — 설치가 만든 것을 역순으로 되돌린다. 데이터는 `--purge-data` 를
  명시해야 지우고, sudo 는 직접 안 쓰고(명령을 찍어 준다), 이 호스트의 override 는
  `~/override.keep.yml` 로 빼 둔다. `--dry-run`·`--keep-images`. README "제거"·QnA

## v0.4.16 — 2026-09-11

NUC 처음 설치 두 번째 시도 — 1절(이미지)은 v0.4.15 대로 됐고 2절(wheel)에서 죽었다.

**설치**
- `docker cp` 가 있는 디렉토리에 **겹쳐** 놓아 `~/piper-web-deploy/latest/wheels/` 에 이전
  시도의 `piper_bus-0.4.13` 이 새 `0.4.15` 옆에 남았고, apply.sh 가 `wheels/*.whl` 을
  통째로 깔다가 pip `ResolutionImpossible`. 두 겹으로: `piper-install.sh` 는 꺼내기 전에
  그 디렉토리를 비운다(이름이 `latest`/`v…` 꼴일 때만 — `current` 는 절대 안 지운다),
  apply.sh 는 매니페스트 버전으로 도장 찍힌 wheel 만 고른다(도장 없는 옛 번들이면 전부).
  그때까지의 우회: `rm -rf ~/piper-web-deploy/<버전> && ./piper-install.sh`
- 세 번째 시도: `Package 'piper-bus' requires a different Python: 3.10.12 not in '>=3.11'` —
  NUC 은 22.04(시스템 파이썬 3.10). wheel 7개가 `>=3.11` 을 **선언**했을 뿐 코드는 3.10 에서
  모든 모듈이 import 되는 것을 `python:3.10` 컨테이너로 실측하고 하한을 3.10 으로 내렸다.
  apply.sh 는 venv 를 만들기 전에 파이썬이 3.10 이상인지 확인하고 아니면 멈춘다(20.04 는
  OS 를 올려야 한다 — README 전제 표에 추가). 시스템에 numpy·opencv 가 없는 맨 우분투는
  venv 에 받는다(있으면 그대로)

## v0.4.15 — 2026-09-11

v0.4.14 를 GPU 없는 NUC 에 **처음** 깔아 보니 — 설치가 아무것도 안 깐다.

**설치**
- `apply.sh` 가 매니페스트의 `images=`/`wheels=`/`daemons=`("이번 릴리스에서 바뀐 것")만
  적용했다 — 직전 릴리스가 깔린 호스트를 전제한 규칙이라, backend 만 든 v0.4.14 를
  처음 설치하는 호스트는 frontend 이미지·venv/wheel·데몬 유닛을 하나도 못 받고 compose
  가 frontend 를 못 찾았다. 이미지 번들엔 셋 다 항상 실려 있으니 이제 **없는 것은
  깐다**: 매니페스트에 없는 이미지는 없을 때만 레지스트리의 `:latest`(마지막으로 구운
  그 이미지)를 받고, venv 가 없거나 실린 wheel 중 안 깔린 게 있으면 전부 깔고, 데몬
  소스·유닛이 없으면 푼다. 이미 깔린 호스트의 동작은 그대로(바뀐 것만)
- `--check` 도 같은 눈으로 본다 — 처음 설치라 받아야 할 것을 "없음"으로 말한다

## v0.4.14 — 2026-09-11

v0.4.13 을 GPU 없는 NUC 에 깔아 보고 드러난 둘 — "설치 한 방"의 구멍.

**설치**
- 처방 `sudo mkdir -p /srv/piper-data && chown …` 의 `&&` 뒤에 sudo 가 없어, 그대로 붙여
  넣으면 chown 이 "Operation not permitted" 였다. 출력은 줄 앞에만 sudo 를 붙인다 —
  뒤에도 붙였고, 처방마다 `&&` 뒤에 sudo 가 있는지 테스트가 본다
- GPU 없는 호스트: compose 가 nvidia 예약을 하드 요구하는데 스크립트는 "GPU 가 안
  보인다"고 경고만 하고 넘겨, 4절 `docker compose up` 이 `could not select device driver
  "nvidia"` 로 죽었다. 이제 nvidia 가 없으면 예약을 지우는 조각(`docker-compose.nogpu.yml`,
  `!reset`)을 배포 디렉토리 `.env` 의 `COMPOSE_FILE` 에 끼운다 — override 파일은 손대지
  않고 있으면 뒤에 붙이며, GPU 호스트는 키를 지워 예전 그대로. 조각은 compose 2.24+ 라
  낮으면 멈추고 올리라고 한다. 학습·추론만 빠지고 수집·시뮬·조종은 된다
- QnA 에 "GPU 없는 기계에 설치하면?", 트러블슈팅에 그 에러 문구 행

**릴리스**
- `release.sh` 가 `apply.sh`·compose 파일·udev·cam/rs/sim wheel 만 바뀐 릴리스를 "바뀐 것이
  없다"로 판정했다 — 그것들은 backend 이미지 안(/opt/piper-host)으로 실려 나가므로
  이미지를 다시 구워야 온라인 설치가 새 것을 받는다. 이 릴리스 자체가 그렇게 막혔다

## v0.4.13 — 2026-09-11

하루치 화면 정리와 설치 안내 — 시뮬 조종 창(블럭 옮기기·도움말), 로그 페이지 통합,
추론 분석, 접속 주소·포트 변수.

**시뮬 조종 창**
- 블럭 옮기기(B): 켜고 탑뷰를 클릭하면 그 자리로 큐브가 순간이동한다 — 그리퍼로
  끌어다 놓기 어려웠던 것. 켜져 있는 동안 클릭은 조종을 시작하지 않는다
- 도움말이 한 줄 텍스트에서 관절·EE·공통을 키/설명 행으로 나눈 패널로([도움말] 버튼
  또는 Shift+/). 관절5 를 T/G 로 적었던 오탐 교정 — T 는 팔 리셋, 관절5 는 가운데
  드래그·휠 선택

**로그**
- "로그"가 둘이었다(사이드바 = /tmp 파일 관리자, 데몬 저널 = 설정 속 탭). 한 페이지
  두 탭으로 — 시스템(저널, 기본)·파일. 설정에서 로그 탭이 빠지고, 서비스 패널의
  유닛별 [로그]는 `/logs?unit=…` 딥링크로 간다
- 탭은 페이지 제목 바로 옆(카메라·로봇과 같은 머리줄) — 규칙으로 잠갔다

**추론 분석** (옛 "디버그")
- 시스템 묶음의 새 창에서 LeRobot 묶음 추론 바로 뒤, 셸 안의 보통 페이지로. 경로
  `/debug` 는 그대로
- 우측 카드의 그래프 칸을 최종 높이로 먼저 잡는다 — 추론 기록 없는 프레임에서 청크
  그래프가 떨어지고 런을 바꾸면 카드가 접히던 것(에피소드 페이지와 같은 원인). 우측
  열도 고정 최소 폭(`twoColumns`)이라 긴 모델 경로·그려진 그래프 폭에 안 밀린다

**설치**
- 끝나면 어디로 가는지: README 에 주소, `apply.sh` 마지막 줄에 실제 IP·포트
- 80 이 아닌 포트는 `PIPER_WEB_PORT=8081 ./piper-install.sh` 한 번 — 배포 디렉토리
  `.env` 에 남아 업데이트에도 유지된다. override 파일 방식도 그대로 동작한다
- `docs/qna.md` — 나온 질문 그대로(접속 주소·다른 포트·설치 뒤 바꾸기)

**그 밖** (v0.4.12 이후 함께 실린 것)
- 제품명 Piper Studio(사이드바·상태바 포함), 대기 상태 라벨 유휴 → 대기중
- 수집·학습·추론의 2열이 내용에 밀려 폭이 바뀌던 것 — 고정 최소 폭(`twoColumns`)
- 버전 카드 값 줄이 카드 밖으로 넘치던 것 — 항목 경계에서 접힌다

## v0.4.12 — 2026-09-10

[버전] 카드가 좁은 화면에서 라벨이 한 글자씩 쪼개져 세로로 늘어졌다
("컨테이너" → "컨/테/이/너").

**버전**
- 값 목록 span에는 `whitespace-nowrap`이 있는데 그 왼쪽 라벨(컨테이너·호스트·데몬)
  에는 없었다. 그리드가 좁아지며 오른쪽 `1fr` 칸에 자리를 내주려 라벨 칸을
  줄이는데, 한글은 공백 없이도 글자 사이에서 줄바꿈되어 한 글자짜리 폭까지
  줄어들었다. 라벨에도 `whitespace-nowrap`을 붙였다

## v0.4.11 — 2026-09-10

v0.4.10을 .120에 웹 버튼으로 올린 뒤 뜬 "게이트웨이는 v0.4.10인데 데몬 wheel이
다릅니다" — 실제 장애가 아니라 이 경고 자체의 오탐이었다.

**버전**
- 경고가 "게이트웨이 버전 == 이번에 올린 모든 wheel 버전"을 전제했는데, 그건
  틀린 전제다. `release.sh`는 바뀐 패키지만 골라 굽는다 — v0.4.8~v0.4.10처럼
  `bus/shm/robot/cam/rs`를 안 건드린 패치에서는 그 wheel들이 예전 버전 그대로인
  게 정상이다. 매니페스트의 `wheels`(이번 릴리스가 실제로 구운 패키지 목록)를
  게이트웨이 응답에 실어, 그 목록에 있는 패키지만 견주도록 고쳤다 — `wheels`가
  비면 아무것도 경고하지 않는다

## v0.4.10 — 2026-09-10

v0.4.9의 받기 수정이 틀렸다 — 진단이 잘못됐다.

**업데이트**
- v0.4.9는 `REPO`(unitd.py 자기 `__file__` 기준, 곧 `current/`) 밑에서
  `piper-install.sh`를 최상위·`deploy/` 밑 둘 다 찾도록 고쳤는데, 실제로는
  **둘 다 아니었다** — `apply.sh`는 `daemons.tar.gz`만 `current/`에 풀어 두고
  `piper-install.sh` 자체는 절대 그 안에 안 온다. 진짜 위치는 받아 둔 버전
  디렉토리(`<WORK>/vX.Y.Z/piper-install.sh`)뿐이라, `_registry()`가 이미 하던
  것과 같은 자리를 보게 고쳤다. .120에서 실제로 웹 [받기]→[적용]까지 API로
  직접 호출해 끝까지 확인했다. 이 버전도 손으로 올려야 한다

## v0.4.9 — 2026-09-10

.120에서 웹 [받기]를 처음 눌러 본 실전 — "이 번들이 낡았다"로 항상 실패.

**업데이트**
- `unitd.update()`의 받기 경로가 저장소 체크아웃 레이아웃(`deploy/piper-install.sh`)
  만 봤다. 배포된 번들(이미지 경로, `stage-hostside.sh`가 최상위에 놓는다)에서는
  그 경로가 없어 매번 거절됐다 — 둘 다 보게 고쳤다. 이 버전도 손으로 올려야 한다
  (그전 unitd에는 이 수정이 없다); 그다음부터는 웹 버튼. ⚠ **이 진단이 틀렸다** —
  v0.4.10 참고

## v0.4.8 — 2026-09-10

리더암 없이 키보드·마우스로 조종하는 시뮬 텔레옵.

**웹 리더** (설정 → 수집의 리더 선택, 로봇 카드의 [조종 창 열기])
- 별도 브라우저 창이 키·마우스를 받아 **가상 리더 세그먼트**를 발행한다 — 릴레이가
  팔로워를 움직이고 수집은 실물 리더와 같은 계약으로 읽는다(시뮬·실기 공통)
- 관절 모드는 손가락 하나에 관절 하나, EE 모드는 마우스가 이동·QWEASD 가 회전이라
  위치·자세를 동시에. 속도 적분은 게이트웨이가 하고 입력이 100ms 끊기면 멈춘다
- 통합기가 매 틱 **실제 시뮬 관절값으로 접지**한다 — 장애물에 막히거나 리셋되면
  목표가 실제에 멈춰 "못 감"으로 굳지 않는다(사용자 보고). 초기화는 순간이동이 아니라
  **원점으로 램프** 복귀라 그 동작도 수집에 담긴다. 도중에 조작하면 인계
- 모든 카메라가 스냅샷 폴링(영구 스트림 0) — 연결 한도로 입력이 굶던 끊김을 없애고,
  영상 fps·입력 전달을 따로 표시해 무엇이 멈췄는지 보인다. 창은 로딩 게이트로 열리고
  시뮬은 느린 하드웨어 스캔을 건너뛴다

**시뮬**
- 집은 물건이 슬슬 미끄러져 빠지던 것 — MuJoCo impratio 를 올리고(1→50) 큐브·손가락에
  비틀림 마찰(condim6)·단단한 접촉을 줘 들고 있어도 안 빠진다
- 탑뷰를 반시계 90°(마우스 EE 면과 정렬)·손목뷰를 시계 90° 회전, 바닥 격자를 또렷하게
- 환경 리셋(큐브 시작 위치+팔 램프 복귀)·팔만 초기화 버튼과 R·T 키
- simd 재시작 뒤 카메라가 빈 화면이던 것 — 스캔이 데몬 사실로 연결 상태를 맞춘다

## v0.4.7 — 2026-09-10

.120 첫 실전 배포에서 걸린 것들 — 시뮬레이터 등록, 새 버전 확인, 설치 진행 표시,
원격 로그.

**시뮬레이터** — 시뮬 팔·카메라가 .120 에서 등록되지 않던 문제
- 씬 XML 이 메시를 저장소 상대경로(`../../../vendor/agx_arm_urdf/...`)로 가리켜서,
  wheel 로 설치하면(site-packages 안) 아무 데도 안 닿았다. 메시를 패키지 안에 담아
  개발 체크아웃·설치본이 같은 상대경로를 쓰게 했다

**버전 확인** — [새 버전 확인] 이 GHCR 같은 공개 레지스트리에서 늘 404
- 사설 레지스트리(`piper-build:5000`, 평문·무인증) 전제로만 짠 URL 이었다. 공개
  레지스트리는 https + `/v2/<조직>/<이미지>/...` 경로 + 토큰 인증이 필요해서,
  사설/공개를 구분해 URL 을 만들고 401 의 `WWW-Authenticate` 챌린지를 따라
  익명 pull 토큰을 받도록 했다. `ghcr.io/wego-robotics` 실기로 검증

**설치 진행 표시**
- 첫 설치가 베이스 이미지 7GB 를 받는 동안 아무것도 안 찍었다 — 유닉스 소켓으로
  도커 pull 스트림을 직접 읽어 레이어별 진행률을 낸다(표준 라이브러리만, pip 없이도
  동작). `docs/install-troubleshooting.md` — 증상으로 찾는 설치 문제 색인 신설

**원격 로그** (설정 → 서비스 → [로그])
- 호스트에 SSH 로 들어가 `journalctl` 을 치지 않아도 웹에서 데몬 저널을 본다.
  유닛 목록이 화면과 백엔드 카탈로그로 갈라져 있던 것도 계약 하나로 통일 —
  `frontend` 로그를 고를 수 없던 빈틈도 메워졌다

**CAN 로그 폭주 방지**
- 버스가 계속 나쁘면(ERROR-WARNING) 10초마다 경고가 쌓여 6시간에 6,281줄까지
  간 적이 있다. 처음은 바로, 그 뒤로는 5분 누적 요약으로 줄인다 — 지우지 않고 센다

## v0.4.6 — 2026-09-10

버전 정보와 웹 업데이트.

**버전** (설정 → 서비스 맨 위)
- 지금 도는 piper-web 버전과 바깥 소프트웨어 — 컨테이너(lerobot·torch·CUDA·piper_sdk), 호스트(드라이버·docker·redis·ollama), 데몬(mujoco·pyrealsense2·piper wheel). 정본은 이미지 매니페스트·git 이고 데몬 것은 자기 보고로 온다 — **온 것만 그린다**
- wheel 에 릴리스 태그가 도장된다(전엔 전부 0.1.0). 게이트웨이와 데몬 wheel 버전이 다르면 노란 줄

**업데이트** — [새 버전 확인] → [받기] → [적용], 받아 둔 다른 버전으로 되돌리기
- 실행은 호스트 `piper-unitd` 가 일시 유닛으로 — 게이트웨이는 절차 마지막에 자기 자신이 갈아치워진다. 받기는 아무것도 실행하지 않고, 적용은 활동 중이면 막고, sudo 가 필요한 전제는 복사 블록으로 보여 준다. 화면은 끊길 것을 알고 돌아오면 새로고침한다
- ⚠ **이 버전으로 올리는 한 번은 손으로** (`piper-install.sh v0.4.6`) — 그 전 unitd 에는 업데이트 창구가 없다. 그다음부터 웹 버튼이다

**배포**
- apply.sh 가 unitd 도 재시작하고 적용본(`current/VERSION`)을 기록한다. 번들에 piper-install.sh·CHANGELOG 가 실린다

## v0.4.5 — 2026-09-09

v0.4.4 를 자른 뒤 같은 날 나온 것들. 서비스 켜기/끄기, 릴레이 해제, SO-101 배포 빈틈.

**서비스** (설정 → 서비스)
- 데몬을 **웹에서 켜고 끄고 "부팅 시 시작"을 고른다** — 호스트의 `piper-unitd` 가 실행한다(컨테이너 게이트웨이는 systemctl 이 없다). estopd 는 상태만 본다
- 시뮬레이션(simd)·SO-101(so101d)은 **모든 기계에 깔리되 꺼진 채** — 재설치해도 켜 둔 선택은 그대로다. 배포 번들에 so101·sim wheel 이 실리고, mujoco·Feetech SDK 는 apply.sh 가 PyPI 에서 깐다(못 깔면 경고만)

**릴레이**
- **[해제]가 팔로워를 놓는다** — 전엔 명령 경로를 쥔 채라 해제 뒤에도 수집·추론·재시작이 "수동 조작 중"으로 막혔다. SO-101 텔레옵 창을 닫으면 릴레이가 끝난다

**배포**
- 게이트웨이 이미지에 `piper_so101` 이 없어 로봇 호스트에서 SO-101 릴레이·수집이 죽을 자리였다 — 넣었다. apply.sh 가 `video`·`dialout` 그룹을 본다
- 변경 이력이 저장소 루트에 있고, 낡으면 테스트가 잡는다

## v0.4.4 — 2026-09-09

시뮬레이션 데몬, SO-101 리더, 로봇 페이지 재구성. 66 커밋 · 112 파일 · 세 레이어 전부.

> ### ⚠ 이 버전은 기존 체크포인트를 무효화한다
>
> joint6 캘리브레이션이 `(-100000, 130000)` 비대칭이라 **정규화 0 이 물리 +15°**
> 였다. piper_sdk 표와 AgileX URDF 둘 다 ±120 이 정본이다. 아홉 곳을 고치고
> 데이터셋 25 개 **339,845 프레임**을 재정규화했다
> (`tools/migrate_j6_calibration.py`).
>
> **2026-09-04 이전에 학습된 정책은 그대로 돌리면 j6 이 10~20° 어긋난다** —
> 재학습 대상이다. 백업은 `~/.cache/huggingface/lerobot-backup-j6-20260904`.

**새 장치**
- MuJoCo **시뮬레이션 데몬** — 팔·카메라 계약을 그대로 말한다. 실기처럼 등록하고 웹에서 조그한다
- **SO-101 리더** — Piper 를 SO-101 로 텔레옵하고 그 상태로 수집한다. 캘리브레이션 마법사 포함. pose 모드는 막았다(모델이 다른 리더끼리는 말이 안 된다)
- CAN 팔과 시리얼 팔이 한 화면을 쓴다 — 포트는 포트, 팔은 로봇

**로봇 페이지** — 디바이스 · 정렬 · 버스 상태 · 버전 · 검사
- **검사**: 관절을 흔들어 재고 서로 견준다. 저장·조회·두 회차 비교. 걸리면 0.1초에 멈추고 리셋을 안내한다
- **정렬**: AprilTag 로 "지난주 대비 틀어졌나". 자세는 조그로 만들고 그 창에서 카메라 영상과 검출된 태그를 같이 본다
- **버스 상태**: 데몬이 2초마다 모아 베이스라인 기준으로 — RX/TX 선, 오류 막대
- **버전**: 팔·관절별 펌웨어

**고친 것**
- **영점 굽기** — 안 굽고도 성공이라 답하던 것을 멈추고, 준비 상태를 기다리고, 설정 프레임이 떨어지므로 재시도한다
- **에피소드 삭제가 조용히 실패하고 있었다** — 유닛 PATH 에 conda bin 이 없어 127 로 즉사했는데 화면은 "삭제 완료" 라고 답했다
- **파킹** — 저장한 자세로 안 갔다(저장은 게이트웨이, 읽기는 robotd 의 다른 폴더). 보정은 세 버튼으로 나눴다
- 카메라 노출을 **선형 광량**으로 재고 측광 위치를 고를 수 있다
- 느린 조명 표류 감지 · 팔·카메라 복귀 시 실종 표시 해제
- **robotd 기동 정리가 살아 있는 so101d 세그먼트를 지우던 것** — 자기 것만 지운다. 발행자는 누가 지워도 세그먼트를 다시 만들고, 수집 시작이 리더 세그먼트를 먼저 열어 본다

## v0.4.3 — 2026-09-02

- **마스터/슬레이브를 팔의 피드백으로 판별**한다 — 우리가 보낸 명령으로 판정하던 것이 원인이었다
- `0x150` 리셋을 **슬립 센서**로 쓰고, 영점 굽기 앞에 세웠다 (틀어짐의 대부분은 영점 손상이 아니라 과부하 슬립이다)
- 로봇 페이지를 한 번에 비우는 버튼
- 화이트밸런스를 직접 재고, 노출 단위를 라벨링하고, 게인이 일하게 둔다
- 조명 감시 — 밝기가 튀면 말한다
- 카메라 프로파일을 작업이 실제로 지키는 계약으로
- HF 네임스페이스 선택기 · C4 구조도

## v0.4.2 — 2026-09-02

- 서비스 패널이 **컨테이너 안에서** 동작한다 — 데몬이 버스로 자가보고
- 자원 추이를 서버가 들고 있어 페이지를 열면 그래프가 차 있다
- 알람을 불투명하고 짧게, 기억하는 종
- 모델·데이터셋 목록으로 들어가는 길을 하나로

## v0.4.1 — 2026-09-01

- 대시보드가 "지금 뭔가 잘못됐나"에 답한다
- 톱니 대신 **로스 곡선**을 그린다. 학습 실행을 먼저 고르고 체크포인트를 고른다
- 개인 설정·머신 주소를 공개 저장소에서 뺐다
- 번들 모양을 하나로, 풀다 실패하면 멈춘다

## v0.4.0 — 2026-09-01

- **Apache-2.0** — 검출기를 RT-DETR 로 바꿔 AGPL 을 걷어냈다
- 설치가 **한 줄** — 호스트가 필요한 것이 전부 이미지 안에 있다
- 레지스트리는 필요할 때만 LAN 에 열고, DHCP 주소가 이미지 위치를 정하지 못하게 했다
- 호스트에 없는 레이어만 보낸다

## v0.3.10 — 2026-08-28

⚠ **git 태그가 없다.** 레지스트리에만 있다 — 이 번호를 diff 기준으로 삼으면 실패한다.

## v0.3.9 — 2026-08-28

원터치 릴리스(`release.sh` / `apply.sh`) 도입. `.120` 완전 삭제 후 재설치.
세 레이어 전부. ⚠ 태그를 소급해서 찍었다(근거는 태그 메시지에).

## v0.3.2 ~ v0.3.8 — 2026-08-15

- gRPC 원격 추론이 **한 번도 못 돌던** `_paused` 버그, 서버 모드 체크포인트 입력
- 멈춘 영상을 정상처럼 보이지 않게 · 스캔 썸네일 오보 제거
- `alert`/`confirm` 30곳을 시스템 메시지로 (추론 중 `confirm` 은 heartbeat 를 막아 E-stop 을 부른다)
- 카메라·robotd 데몬이 장치 사라짐을 **스스로 판정**한다

## v0.2.7 ~ v0.3.1 — 2026-08-14

장치 사라짐 경보 — "발행이 멈췄나"로 판정을 좁히고, 재시작 시점에 이미 멈춰 있던
장치도 잡고, 뽑힌 카메라를 스캔 목록에서 뺀다.

## v0.2.0 — 2026-08-14

데몬 분리 + 배포용 docker-compose 구조 개편. `.120` 최초 전체 배포.

## v0.1.0 — 2026-08-13

MVP.
